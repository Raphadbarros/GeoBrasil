package com.desafiojava.mapearestado.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RestController;

import com.desafiojava.mapearestado.services.EstadosServices;

@RestController
public class MapearestadoController {

	@Autowired
	private EstadosServices estadosServices;

	@RequestMapping(value = "/pessoa", method = RequestMethod.GET)
	public String test() {
		return "Rest";
	}

	/*
	 * @RequestMapping(value = "/id", method = RequestMethod.GET) public
	 * ResponseEntity<?> listarConsorcioId(@PathVariable Integer id) throws
	 * ObjectNotFoundException { Estados obj = estadosServices.buscarEstadosId(id);
	 * return ResponseEntity.ok().body(obj); }
	 * 
	 */
}
